package com.autonomousdroneexpert.m1c.ui

/** The screens of the harness. Navigation is a simple, explicit state machine. */
enum class Screen {
    Identification,
    SafetyGate,
    Devices,
    Tests,
    Results,
}
