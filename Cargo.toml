# Cargo workspace for the independent Rust core.
#
# M1 uses first-party workspace path dependencies only. External production dependencies
# remain prohibited and are checked by ADR-0009 policy plus cargo-deny. Each crate keeps a
# narrow ownership boundary; adding any external dependency requires a dedicated audit.

[workspace]
resolver = "3"
members = ["crates/*"]

[workspace.package]
version = "0.0.0"
edition = "2024"
# Documented MSRV. This is NOT the pinned toolchain: rust-toolchain.toml pins the exact
# toolchain used to build and test. See docs/adr/ADR-0002.
rust-version = "1.85.0"
publish = false

# No [profile.release] here on purpose. Release profile choices -- panic strategy, debug
# information, optimisation level -- are real engineering decisions with consequences for
# unwinding, recovery and diagnostics. They are deferred to a dedicated ADR taken when
# there is something to release, not inherited from a template.
