[package]
name = "ade-fault-injection"
description = "Deterministic fault injection harness: timeouts, disconnects, power loss and process kill."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# First-party workspace path dependency only (ADR-0009).
[dependencies]
ade-transport = { path = "../transport" }
