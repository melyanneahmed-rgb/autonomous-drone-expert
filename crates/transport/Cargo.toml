[package]
name = "ade-transport"
description = "Physical link abstraction: serial ports and USB enumeration behind replaceable traits. Only crate permitted to hold OS-specific code."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# First-party workspace path dependency only (ADR-0009).
[dependencies]
ade-protocol-msp = { path = "../protocol-msp" }
ade-runtime-ports = { path = "../runtime-ports" }
ade-safety = { path = "../safety" }
