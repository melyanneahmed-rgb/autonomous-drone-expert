[package]
name = "ade-mock-fc"
description = "Mock flight controller simulating firmware versions, boards and fault conditions."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# First-party workspace path dependencies only (ADR-0009).
[dependencies]
ade-protocol-msp = { path = "../protocol-msp" }
ade-transport = { path = "../transport" }
