[package]
name = "ade-protocol-cli"
description = "Firmware CLI channel, including session-state transitions and MSP resynchronisation."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# No dependencies. Adding one requires its own reviewed pull request.
[dependencies]
