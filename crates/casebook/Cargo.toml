[package]
name = "ade-casebook"
description = "Crash-safe local case recording under the privacy constraints. Local only, never uploaded."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# First-party workspace path dependencies only (ADR-0009).
[dependencies]
ade-facts = { path = "../facts" }
ade-safety = { path = "../safety" }
ade-backup = { path = "../backup" }
ade-runtime-ports = { path = "../runtime-ports" }
