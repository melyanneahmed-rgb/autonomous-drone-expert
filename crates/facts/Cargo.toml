[package]
name = "ade-facts"
description = "Fact and confidence model: value, source, timestamp, firmware scope and proof method."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# First-party workspace path dependency only (ADR-0009).
[dependencies]
ade-protocol-msp = { path = "../protocol-msp" }
