[package]
name = "ade-session"
description = "Request and response session management: queueing, timeouts, cancellation and reconnection."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# First-party workspace path dependency only (ADR-0009).
[dependencies]
ade-safety = { path = "../safety" }
